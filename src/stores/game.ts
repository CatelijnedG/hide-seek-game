import { create } from 'zustand';\nimport { Game, Player, GameState, PlayerRole } from '@/lib/types';\n\ninterface GameStore {\n  gameId: string | null;\n  lobbyCode: string | null;\n  gameState: GameState;\n  currentPlayer: Player | null;\n  hider: Player | null;\n  seekers: Player[];\n  allPlayers: Player[];\n  winningTeam: PlayerRole | null;\n  gameStartTime: Date | null;\n  hideEndTime: Date | null;\n  gameEndTime: Date | null;\n\n  // Actions\n  setGameId: (id: string) => void;\n  setLobbyCode: (code: string) => void;\n  setGameState: (state: GameState) => void;\n  setCurrentPlayer: (player: Player) => void;\n  setHider: (player: Player) => void;\n  setSeekers: (seekers: Player[]) => void;\n  setAllPlayers: (players: Player[]) => void;\n  setWinningTeam: (team: PlayerRole) => void;\n  setGameTimes: (startTime: Date, hideEndTime: Date, gameEndTime: Date | null) => void;\n  reset: () => void;\n}\n\nconst initialState = {\n  gameId: null,\n  lobbyCode: null,\n  gameState: 'LOBBY' as GameState,\n  currentPlayer: null,\n  hider: null,\n  seekers: [],\n  allPlayers: [],\n  winningTeam: null,\n  gameStartTime: null,\n  hideEndTime: null,\n  gameEndTime: null,\n};\n\nexport const useGameStore = create<GameStore>((set) => ({\n  ...initialState,\n\n  setGameId: (id) => set({ gameId: id }),\n  setLobbyCode: (code) => set({ lobbyCode: code }),\n  setGameState: (state) => set({ gameState: state }),\n  setCurrentPlayer: (player) => set({ currentPlayer: player }),\n  setHider: (player) => set({ hider: player }),\n  setSeekers: (seekers) => set({ seekers }),\n  setAllPlayers: (players) => set({ allPlayers: players }),\n  setWinningTeam: (team) => set({ winningTeam: team }),\n  setGameTimes: (startTime, hideEndTime, gameEndTime) =>\n    set({ gameStartTime: startTime, hideEndTime, gameEndTime }),\n  reset: () => set(initialState),\n}));\n"